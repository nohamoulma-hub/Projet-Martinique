# Modèle de fichier .env : copier en .env (non versionné) et adapter si besoin.
APP_NAME=Martinique API
ENVIRONMENT=development
# Origines autorisées à appeler l'API (frontend local), séparées par des virgules.
CORS_ORIGINS=http://localhost:5500,http://127.0.0.1:5500
# Clé secrète pour signer les tokens JWT : générer une valeur aléatoire robuste en production.
JWT_SECRET_KEY=changeme
JWT_EXPIRE_HOURS=24
# URL de connexion à la base de données.
# Avec docker-compose : "db" est le nom du service PostgreSQL, résolu par le DNS interne de Docker.
DATABASE_URL=postgresql://martinique:changeme@db:5432/martinique
# Sans Docker, pour un lancement local rapide, remplacer par :
# DATABASE_URL=sqlite:///./martinique.db
