# Fichiers à ne jamais committer : environnement virtuel, caches Python, secrets.
venv/
__pycache__/
*.pyc
.env

# Fichier de base de données local (généré par les migrations Alembic, pas du code).
*.db
